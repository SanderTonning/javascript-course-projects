//challenge:

// Create age set to your age
// calculate is child - if they are 7 or under
// Calculate senior - if thye are 65 or older
// Print is child value
// Print is senior value

let age = 4,
    isChild = 7,
    isSenior = 65;

// console.log(isChild);
// console.log(isSenior);

// New task: Write with if statement

if (age <= isChild) {
    console.log('Your age is ' + age + ', this qualifies you for a childrens discount!')
} 
if (age >= isSenior) {
    console.log('Your age is ' + age + ', this qualifies you for a seniors discount!')
}

